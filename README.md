
# Binary Digit Recognition in MATLAB

A Convolutional Neural Network (CNN) trained in MATLAB to classify handwritten binary digits (`0` and `1`). 

## Project Features
- Uses an adaptive binarization preprocessing pipeline to handle noisy custom images.
- Augmented training data using translation and rotation layers.
- Custom filename parsing for automated datastore labeling.
